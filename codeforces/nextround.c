#include <stdio.h>

// http://codeforces.com/problemset/problem/158/A

int main(int argc,char * argv[])
{
  // constestant who earns a score equal to or greater 
  // than the kth place finishers score will advance 
  // to the next round 
  // the input is 

  int n;
  int k;
  int* arr;

  scanf("%d %d",&n,&k);
  arr = (int*) malloc((n - 1) * sizeof(int));
  

}